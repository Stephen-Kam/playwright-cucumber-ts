import { BasePage } from './BasePage';

/**
 * Page object for example.com.
 * Add locators/actions specific to this page here as scenarios grow, e.g.:
 *
 *   private readonly moreInfoLink = this.page.getByRole('link', { name: 'More information...' });
 *   async clickMoreInfo(): Promise<void> { await this.moreInfoLink.click(); }
 */
export class ExamplePage extends BasePage {}
